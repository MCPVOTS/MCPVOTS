// Polyfills for SSR compatibility - disabled to avoid indexedDB issues
// indexedDB polyfills are causing issues, so we'll disable recent transactions instead
console.log('ℹ️ indexedDB polyfills disabled - using RainbowKit without recent transactions')
